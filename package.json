{
  "name": "void-drift-monorepo",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "check": "pnpm -r check",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.16",
    "typescript": "^5.0.0",
    "vitest": "^4.0.16"
  }
}