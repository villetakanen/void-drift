{
  "name": "void-drift-monorepo",
  "private": true,
  "version": "0.0.4",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}